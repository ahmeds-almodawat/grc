# Gate 12R dedicated staging target contract

Create a separately named `grc-staging` Vercel project with provider-managed public staging variables, exact Supabase project isolation, Patch 83U credential governance, CAPTCHA configuration for its approved origin, deployment protection, commit metadata and rollback. It must have no production domain, promotion, secret/service-role value or `.env` upload.
