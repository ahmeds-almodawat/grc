# Gate 12 hosted staging environment contract

The future hosted build must derive exactly staging project `zghsgzrdwbqdrpuxanac`, reject production and unknown refs before build, enable Patch 83U credential governance, use only a provider-managed public browser key, preserve project-isolated Auth storage, configure CAPTCHA consistently, and record the release commit SHA. No service-role or secret key may enter frontend configuration.
