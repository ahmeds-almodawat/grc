# Release Proof Evidence

This folder contains generated JSON reports from our CI validation scripts (Schema, Workflow, Frontend, and Runtime Security).

- Folders here represent historical and current deployment iterations (`v*` or `patch*`).
- **Do not delete these folders**. They serve as our audit trail for secure deployments and accreditation evidence.
