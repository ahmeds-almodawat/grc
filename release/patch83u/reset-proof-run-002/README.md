# Patch 83U reset proof run 002

This directory is reserved for a future, separately authorized hosted staging
execution against Supabase project `zghsgzrdwbqdrpuxanac`.

The prior authorization is exhausted. This reservation is not execution
authorization and contains no success evidence.

The harness output path reserved for that future run is:

`release/patch83u/reset-proof-run-002/patch83u-staging-reset-final-results.json`

The result file is intentionally absent. The harness must create it with
exclusive-create semantics and must refuse to overwrite any existing artifact.
