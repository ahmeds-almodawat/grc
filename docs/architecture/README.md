# Architecture Documentation

This directory contains technical architecture documentation.

- The application uses React, Vite, and Supabase.
- Edge-bridge architecture is used for mutating security-sensitive data via Supabase RPCs.
- Row-Level Security (RLS) is strictly enforced on all tables.
